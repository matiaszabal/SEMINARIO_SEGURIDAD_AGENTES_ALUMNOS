# Dependencias para correr los labs de Capítulo 1 y 2 (labs/ch01-lab*, ch02-lab*).
#
# google-adk: framework de agentes usado por todos los scripts (Agent, Runner,
# InMemorySessionService).
# google-genai: tipos de contenido (google.genai.types) usados por ADK, y
# requerido por la variante -gcp para hablar con Gemini.
# litellm: requerido por la variante -ollama, que envuelve el modelo local
# (qwen3.5:9b vía Ollama) con LiteLlm(model="ollama_chat/...") -no es una
# dependencia transitiva de google-adk, hace falta instalarlo aparte.
google-adk>=2.2.0
google-genai>=1.0.0
litellm>=1.60.0

# Para correr las variantes -ollama: instalar Ollama (https://ollama.com) y
# descargar el modelo local con `ollama pull qwen3.5:9b` antes de correr los
# scripts.
#
# Para correr las variantes -gcp: necesitás un proyecto de Google Cloud con
# Vertex AI habilitado y credenciales configuradas (`gcloud auth
# application-default login`), o una API key de Gemini via AI Studio.

# Capítulo 2 — Lab 2.1 (MAESTRO Automático): valida la salida estructurada
# del agente contra un schema Pydantic.
pydantic>=2.6.0

# Capítulo 2 — Lab 2.2 (RAG Poisoning): vector store local. Los embeddings
# por defecto de ChromaDB corren vía ONNX local (all-MiniLM-L6-v2, sin API
# ni conectividad en tiempo de query) -no hace falta sentence-transformers.
chromadb>=0.4.22

# Capítulo 2 — Lab 2.3 (Observabilidad MAESTRO): Cloud Logging de GCP es
# opcional -el lab corre 100% local con print() si no hay credenciales o si
# el paquete no está instalado (fallback silencioso, ver el .py).
# google-cloud-logging>=3.0.0
